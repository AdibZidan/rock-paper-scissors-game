export enum ViewType {
  MODE_SELECTOR = 'modeSelector',
  HEADER = 'header',
  ORIGINAL = 'original',
  BONUS = 'bonus',
  ARENA = 'arena',
  BATTLEGROUND = 'battleground',
  MODAL = 'modal',
  FOOTER = 'footer'
}
